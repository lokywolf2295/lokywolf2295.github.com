package logica;

import igu.Pantalla;

public class PruebaCapas {

    public static void main(String[] args) {
        
        Pantalla panta = new Pantalla (); //creamos el objeto con la variable de la clase pantalla
        panta.setVisible(true);//permite que la capa visual sea visible
        panta.setLocationRelativeTo(null); //pone la pantalla sin relativos (Centra la pantalla)
        
        
    }
    
}
